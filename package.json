{
  "name": "miroms",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n web,admin -c blue,green \"pnpm --dir app/web dev\" \"pnpm --dir app/admin dev\"",
    "dev:web": "pnpm --dir app/web dev",
    "dev:admin": "pnpm --dir app/admin dev"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
